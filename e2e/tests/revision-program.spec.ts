import { test } from '@playwright/test'

test.describe('Revision Program (Phase 7A — Foundation)', () => {
  test.skip('teacher can access revision program route when built', async ({ page }) => {
    // UI built in Phase 7B — skip for now
    // test: login as j.patel@omnisdemo.school, navigate to /revision-program
  })

  test.skip('getClassPerformanceAnalysis returns data', async () => {
    // Full unit tests in Phase 7C
  })
})
